use: {
    "core.array"
    "core.builtin.misc"
    "core.mixin"
    "core.sequence"
}

MutableSequence mix-in-to: Array

let: (mutable-like: (_: Array) length: n) do: [ n nulls-array ]
let: ((seq: Array) like: (_: Array)) do: [ seq ]
