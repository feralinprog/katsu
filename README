Quick start:
```bash
./run
```
This builds all, tests everything, and runs the main Katsu executable.

To only build:
```bash
./run b
```

To build and run tests:
```bash
./run t
```

To build and run the main Katsu executable:
```bash
./run k
```